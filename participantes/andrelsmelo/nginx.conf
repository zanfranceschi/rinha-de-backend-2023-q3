events {
    worker_connections 2048;
}
http {
    upstream api {
        keepalive 200;
        server go-api1:8080;
        server go-api2:8080;
    }
    server {
        listen 9999;
        location / {
            proxy_pass http://api;
        }
    }
}